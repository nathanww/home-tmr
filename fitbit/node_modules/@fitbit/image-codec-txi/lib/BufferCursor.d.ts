export default class BufferCursor {
    length: number;
    private p;
    array: Uint8Array;
    constructor(length: number);
    get buffer(): ArrayBuffer | SharedArrayBuffer;
    seek(offset: number): void;
    tell(): number;
    slice(from?: number, to?: number): ArrayBuffer;
    writeArray(arr: Uint8Array): void;
}
