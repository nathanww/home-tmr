"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const lodash_1 = tslib_1.__importDefault(require("lodash"));
function byteStringFormatter(byteCount, decimalCount = 4) {
    if (byteCount === 0) {
        return '0 B';
    }
    const unitSize = 1024;
    const unitNames = ['B', 'KB', 'MB', 'GB', 'TB', 'PB', 'EB', 'ZB', 'YB'];
    const index = Math.floor(Math.log(byteCount) / Math.log(unitSize));
    return `${parseFloat((byteCount / Math.pow(unitSize, index)).toFixed(lodash_1.default.clamp(decimalCount, 0, 20)))} ${unitNames[index]}`;
}
exports.default = byteStringFormatter;
