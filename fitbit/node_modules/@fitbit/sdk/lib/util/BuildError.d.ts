import { Diagnostic, DiagnosticTarget } from '../diagnostics';
export default class BuildError extends Error {
    target?: DiagnosticTarget;
    static is(error: Error): error is BuildError;
    toDiagnostic(): Diagnostic;
}
