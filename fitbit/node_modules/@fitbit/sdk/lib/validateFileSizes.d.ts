/// <reference types="node" />
import stream from 'stream';
import { DiagnosticHandler } from './diagnostics';
export default function validateFileSizes({ maxSizes, onDiagnostic, }: {
    maxSizes: Record<string, number | undefined>;
    onDiagnostic: DiagnosticHandler;
}): stream.Transform;
