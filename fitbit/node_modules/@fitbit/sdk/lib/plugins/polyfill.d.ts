import { Plugin } from 'rollup';
export declare type Polyfill = string | (() => string | Promise<string>);
export declare type PolyfillMap = Record<string, Polyfill>;
export default function polyfill(modules: PolyfillMap): Plugin;
