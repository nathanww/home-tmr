"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const common = [
    'cbor',
    'crypto',
    'device',
    'events',
    'file-transfer',
    'geolocation',
    'messaging',
    'permissions',
    'resources',
    'user-settings',
    'util',
];
const device = [
    'accelerometer',
    'appbit',
    'barometer',
    'body-presence',
    'clock',
    'display',
    'document',
    'exercise',
    'fs',
    'gyroscope',
    'haptics',
    'heart-rate',
    'jpeg',
    'orientation',
    'power',
    'scientific',
    'scientific/signal',
    'sensors',
    'system',
    'user-activity',
    'user-profile',
].concat(common);
const companion = [
    'app-cluster-storage',
    'calendars',
    'companion',
    'external-app',
    'image',
    'local-storage',
    'mobile-notifications',
    'peer',
    'secure-exchange',
    'settings',
    'user-water',
    'user-weight',
].concat(common);
const settings = ['user-settings'];
const externals = { device, companion, settings };
function platformExternals(component) {
    return {
        name: 'platform-externals',
        resolveId(importee) {
            if (importee.startsWith('internal/'))
                return false;
            if (externals[component].indexOf(importee) !== -1)
                return false;
        },
    };
}
exports.default = platformExternals;
