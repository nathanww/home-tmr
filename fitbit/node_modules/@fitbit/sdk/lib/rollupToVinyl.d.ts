/// <reference types="node" />
import { Readable } from 'stream';
import * as rollup from 'rollup';
export default function rollupToVinyl(inputOptions: rollup.RollupOptions, outputOptions: rollup.OutputOptions): Readable;
