var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
import ErrorSubclass from 'error-subclass';
import { failure } from 'io-ts/lib/PathReporter';
import { pipe } from 'fp-ts/lib/pipeable';
import { fold } from 'fp-ts/lib/Either';
var DecodeError = (function (_super) {
    __extends(DecodeError, _super);
    function DecodeError() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    DecodeError.displayName = 'DecodeError';
    return DecodeError;
}(ErrorSubclass));
export { DecodeError };
export function decode(type) {
    return function (value) {
        return pipe(type.decode(value), fold(function (errors) {
            throw new DecodeError(failure(errors).join('\n'));
        }, function (tag) { return tag; }));
    };
}
